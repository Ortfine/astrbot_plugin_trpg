# 更新日志

## v0.5.4
- 并发安全加固：自动家务进度文件（auto_state.json）改为带锁原子读-改-写，多会话同时对话不再互相覆盖进度计数
- 自动提炼写回角色卡前重新读卡合并（与总结剧情同款修复），不再覆盖提炼期间的状态栏快照/手动编辑
- 角色卡原子写的临时文件改用唯一文件名，同一张卡并发保存时不再互相踩踏
- `/改卡` 支持带空格的卡名（在卡名列表里最长前缀匹配，如「迷雾 老板娘」）
- 采访式 AI 建卡/改卡增加硬上限：模型反复返回坏格式时自动中止，防止无限消耗 token
- 修复测试脚本 test_v050.py 计数分母笔误

## v0.5.3
- 重roll：`/重roll` 把最后一条 AI 回复从记忆中删除，用完全相同的上下文（注入+世界书命中）重新生成——AI 答歪了一键重骰
- 带完整历史上下文直调 LLM（v4.5.7+ 的 `llm_generate` contexts 参数，老版本自动回退 `provider.text_chat`）
- 重roll 成功后手动补状态栏快照、记世界书日志，保持记录链完整；失败/超时历史原样不动
- 状态栏快照解析逻辑提取为可复用方法（钩子与重roll共用）

## v0.5.2
- 采访式 AI 建卡：`/AI建卡` 糊入杂乱素材，AI 反问澄清需求（最多 6 轮）→ 生成完整角色卡预览（含动态世界书条目）→ 确认「是」才保存
- 采访式改卡：`/改卡` 同款引擎重做，反问澄清 → 方案预览 → 确认才写入，默认追加不覆盖
- 不再依赖 agent 工具链，全版本 AstrBot 兼容
- 自动快照：每 N 轮对话自动把当前历史存入槽位 8~10 轮替（保留最近 3 份），配置面板开启
- 自动提炼：世界书日志每新增 N 轮，后台自动蒸馏设定写入角色卡世界观，完成后发会话通知，配置面板开启
- `/回滚 3-5` 支持砍掉从最近数第 3 到第 5 轮（中间轮），不再只能砍尾部
- `/破甲 自定义 <文本>` 支持自定义破甲文本（之前只能在 YAML 里手写）
- 数据安全加固（经 DeepSeek 独立审查后修复）：
  - 角色卡原子写（临时文件+替换），损坏 YAML 读卡不再让注入钩子抛异常
  - 自动快照跳过被手动存档占用的槽位，不再静默覆盖
  - 总结前备份槽满时优先覆盖自动快照槽，而不是直接顶槽位 10
  - 总结剧情写回前重新读卡合并，不再回滚等待期间的状态栏快照/手动编辑
  - 世界书日志按角色卡隔离提炼，切卡后蒸馏不再混入上张卡的剧情
  - 自动提炼失败会回退计数自动重试并通知，不再静默消耗机会
  - 蒸馏输出含异常标记时拒绝写入；开场白重复追加防护；打错指令拦截限定作用域
- 记录块防膨胀覆盖「设定提炼」「自动提炼」块（各自上限 5 块）
- 补充 CHANGELOG.md，消除插件管理器警告

## v0.5.1
- 破甲沉浸协议：`/破甲` 开关，随角色卡注入保持沉浸不出戏，支持 `jailbreak_prompt` 自定义
- 单字段编辑增量模式：发「+内容」直接追加，告别复制原文
- `/改卡` AI 助手改卡（v0.5.2 起升级为采访式引擎）

## v0.5.0
- 主动性模式（主动带节奏/被动跟随/攻守兼备）角色卡字段、创建向导第 7 步、模板默认值
- 状态栏：AI 每条回复末尾输出状态栏，快照自动存回角色卡，📅时间自动同步游戏时间；`/状态栏` 开关与自定义模板、`/状态` 查看快照
- 动态世界书：`lore_entries` 关键词触发按需注入，`/世界书` 三步引导管理
- 快速创建松绑：字段别名、乱序与缺省容错、同名字段合并，识别失败可用 AI 把自由文本整理成卡

## v0.4.0
- 开场白（模板/创建/编辑全流程）
- 世界书日志（逐轮自动记录）+ `/查档` + `/提炼设定`
- `/删档` 指令；`/存档 <槽位> [备注]` 直接参数；存档确认支持「存」自动选空槽
- 修复 `/总结剧情` LLM 调用无超时导致永久卡死、自动备份无脑覆盖槽位 10 的问题
- 剧情记录块防膨胀（上限 5 块）；帮助文本与打错指令拦截补全

## v0.3.x
- 玩法模式（AI扮演/玩家扮演/纯叙事者）；符号系统（内心/旁白/时间流逝）
- NPC/地点/剧情主线字段注入；`/总结剧情`（分段总结、JSON 提取）
- 槽位制存档 + 分享码；打错指令拦截；未存档提醒；数据库自动迁移

## v0.2.x
- 配置面板、白名单/人格标记注入模式、管理员指令
- 6 个预设模板、一次性粘贴创建、中文短指令
- 修复切换角色卡时旧对话历史污染新设定的问题
